name: firebase_query_builder_example
description: Example app demonstrating firebase_query_builder usage.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_query_builder:
    path: ../
